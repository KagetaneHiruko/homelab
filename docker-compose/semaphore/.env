# MySQL Passwort für den User "semaphore"
SEMAPHORE_MYSQL_PASSWORD=MySQLPasswort
# Admin Passwort für den User "Admin"
SEMAPHORE_ADMIN_PASSWORD=AdminPasswort
# Schlüssel zum Verschlüsseln sensibler Daten in der Datenbank, muss mit folgendem Befehl generiert werden: head -c32 /dev/urandom | base64
SEMAPHORE_ACCESS_KEY_ENCRYPTION=AccessKeyForEncryption